{
  "name": "vscode-claude-shim-launcher",
  "private": true,
  "type": "module",
  "workspaces": [
    "ui"
  ],
  "scripts": {
    "dev": "cross-env LAUNCHER_PORT=4590 npm run dev --workspace=ui",
    "start": "cross-env NODE_ENV=production LAUNCHER_PORT=4590 npm run start --workspace=ui",
    "build": "npm run build --workspace=ui",
    "build:api": "tsc -p tsconfig.json",
    "test": "npm run build:api && node --test .tsbuild/lib/*.test.js"
  },
  "dependencies": {
    "cross-env": "^10.1.0",
    "dockerode": "^5.0.1",
    "next": "^15.5.4",
    "react": "^19.2.0",
    "react-dom": "^19.2.0"
  },
  "devDependencies": {
    "typescript": "^5.9.2"
  }
}
